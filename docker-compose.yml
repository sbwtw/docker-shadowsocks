version: '3'
services:
    ssserver:
        image: ssserver
        container_name: ssserver
        build: .
        volumes:
            - ./ssserver:/ssserver
        command: sh -c "/server.sh"
        environment:
            - HOST_DOMAIN
        ports:
            - "443:443"
